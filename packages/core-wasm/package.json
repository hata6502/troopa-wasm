{
  "name": "core-wasm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release --target wasm32-unknown-unknown",
    "fix": "cargo fmt",
    "test": "cargo test"
  }
}
